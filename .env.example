# Copy to .env.local for local dev (never commit real secrets).

# Mobile chat (POST /api/mobile/chat) — NVIDIA NIM via integrate.api.nvidia.com
# NVIDIA_NIM_API_KEY=
# Optional; defaults to a small Llama instruct model on the NVIDIA API catalog.
# NVIDIA_NIM_MODEL=meta/llama-3.1-8b-instruct
